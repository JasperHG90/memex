{
  "id": "memory-memex",
  "kind": "memory",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "serverUrl": {
        "type": "string"
      },
      "searchLimit": {
        "type": "number",
        "minimum": 1,
        "maximum": 50
      },
      "tokenBudget": {
        "type": "number",
        "minimum": 100,
        "maximum": 50000
      },
      "defaultTags": {
        "type": "string"
      },
      "vaultId": {
        "type": "string"
      },
      "timeoutMs": {
        "type": "number",
        "minimum": 500,
        "maximum": 30000
      },
      "minCaptureLength": {
        "type": "number",
        "minimum": 1,
        "maximum": 1000
      },
      "autoRecall": {
        "type": "boolean"
      },
      "autoCapture": {
        "type": "boolean"
      },
      "profileFrequency": {
        "type": "number",
        "minimum": 1,
        "maximum": 500
      },
      "captureMode": {
        "type": "string",
        "enum": ["filtered", "full"]
      },
      "sessionGrouping": {
        "type": "boolean"
      }
    }
  },
  "uiHints": {
    "serverUrl": {
      "label": "Memex Server URL",
      "placeholder": "http://localhost:8000",
      "help": "Base URL of the Memex REST API"
    },
    "searchLimit": {
      "label": "Search Limit",
      "placeholder": "8",
      "help": "Max memory results per recall query"
    },
    "tokenBudget": {
      "label": "Token Budget",
      "help": "Maximum token budget for memory retrieval results. Controls how many memories are returned via greedy packing. Leave unset to use server default (2000).",
      "advanced": true
    },
    "defaultTags": {
      "label": "Default Tags",
      "placeholder": "agent,openclaw",
      "help": "Comma-separated tags applied to captured notes"
    },
    "vaultId": {
      "label": "Vault ID",
      "help": "Restrict search/capture to a specific vault (optional)",
      "advanced": true
    },
    "timeoutMs": {
      "label": "Recall Timeout (ms)",
      "placeholder": "5000",
      "help": "Timeout for the memory search step",
      "advanced": true
    },
    "minCaptureLength": {
      "label": "Min Capture Length",
      "placeholder": "50",
      "help": "Minimum user message length to trigger capture",
      "advanced": true
    },
    "autoRecall": {
      "label": "Auto-Recall",
      "help": "Automatically inject relevant memories into context before each turn"
    },
    "autoCapture": {
      "label": "Auto-Capture",
      "help": "Automatically store conversation turns in Memex after each turn"
    },
    "profileFrequency": {
      "label": "Profile Frequency",
      "placeholder": "20",
      "help": "Inject entity/knowledge-graph context every N turns. Lower = more context but more tokens.",
      "advanced": true
    },
    "captureMode": {
      "label": "Capture Mode",
      "help": "'filtered' captures only user messages (default). 'full' captures both user and assistant.",
      "advanced": true
    },
    "sessionGrouping": {
      "label": "Session Grouping",
      "help": "Group turns into a single session document instead of one note per turn.",
      "advanced": true
    }
  }
}
