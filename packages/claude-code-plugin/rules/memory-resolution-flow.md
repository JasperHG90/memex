## When the user reports an issue resolved

The 5-step resolution flow (disambiguate → route → judge → paired writes), the orthogonal-axes table, and the historical / audit-query routing rule live inside the `memex_record_outcome` and `memex_memory_deprioritize` MCP tool descriptions. Both descriptions teach the same flow because both verbs participate in it.

Read those tool descriptions before mutating memory. They are the contract.
