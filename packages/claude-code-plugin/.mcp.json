{
  "mcpServers": {
    "memex": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "uvx --from \"memex-cli[mcp] @ git+https://github.com/JasperHG90/memex.git@${MEMEX_PLUGIN_VERSION:-latest}#subdirectory=packages/cli\" memex mcp run"
      ]
    }
  }
}
