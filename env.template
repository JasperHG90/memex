# Memex Homelab — fill in your secrets below
# Copy this file to .env:  cp env.template .env
# .env is gitignored. Never commit it.

# PostgreSQL password (used by both db and api services)
MEMEX_DB_PASSWORD=CHANGE_ME

# Optional: Anthropic API key (only if switching from Ollama to Anthropic)
# ANTHROPIC_API_KEY=sk-ant-...
