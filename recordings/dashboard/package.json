{
  "name": "memex-recordings",
  "private": true,
  "type": "module",
  "scripts": {
    "record:overview": "tsx scripts/record-overview.ts",
    "record:entity-graph": "tsx scripts/record-entity-graph.ts",
    "record:memory-search": "tsx scripts/record-memory-search.ts",
    "record:knowledge-flow": "tsx scripts/record-knowledge-flow.ts",
    "record:lineage": "tsx scripts/record-lineage.ts",
    "record:all": "npm run record:overview && npm run record:entity-graph && npm run record:memory-search && npm run record:knowledge-flow && npm run record:lineage"
  },
  "devDependencies": {
    "playwright": "^1.40.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
